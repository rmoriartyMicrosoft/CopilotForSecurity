openapi: 3.0.0
info:
  title: MD5 Hash Lookup API (BrokerNitrix)
  version: "1.0"
servers:
  - url: https://prod-89.eastus.logic.azure.com:443
paths:
   /workflows/8c84a5f131e44b949ae4564292cad00e/triggers/When_a_HTTP_request_is_received/paths/invoke:    
     post:
      operationId: MD5HashBroker
      description: Triggers the specified Logic App workflow.
      summary: Retrieves the original value of a given MD5 hash (BrokerNitrix)
      parameters:
        - name: api-version
          in: query
          default: 2016-10-01
          schema:
            type: string
        - name: sp
          in: query
          schema:
            type: string
        - name: sv
          in: query  
          default: 1.0
          schema:
            type: string
        - name: sig
          in: query
          schema:
            type: string
      requestBody:
        description: The MD5 hash to look up
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                MD5:
                  type: string
      responses:
        "200":
        description: Information about the hash, whether found or not.
